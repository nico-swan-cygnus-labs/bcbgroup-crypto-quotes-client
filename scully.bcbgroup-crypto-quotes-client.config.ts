import { ScullyConfig } from '@scullyio/scully';
export const config: ScullyConfig = {
  projectRoot: "./src",
  projectName: "bcbgroup-crypto-quotes-client",
  outDir: './dist/static',
  routes: {
  }
};